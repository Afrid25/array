const numbers = [23, 32, 53, 54 ,56];

console.log(numbers);

numbers.shift(); // remove index from first.

console.log(numbers);

numbers.unshift(45); // unshift added from first 

console.log(numbers);